# PR 8 review: disposition of every finding

Review of head `c27f2b0` (one summary review, 18 inline comments, two comments without a line, three process points). Each comment was treated as a **claim to verify**, not as an instruction: it was reproduced first, then fixed with a regression test, or recorded below as deferred or not applicable with the evidence. Nothing here changes scope, opens a gate, approves a rights row, or is a reply on the pull request. Review threads are left for the reviewer to resolve.

Status words: **fixed** (reproduced, changed, regression-tested), **fixed, human step remains** (the code now fails closed; a named person must still act), **corrected** (the claim did not match the facts, which are recorded), **deferred** (not this change), **not changed** (evaluated, with reasons).

All verification was on the task-local disposable database. No hosted project was touched, nothing was pushed or deployed, no schedule is active.

## Should-fix: security

| # | Finding | Verified? | Disposition | Evidence |
|---|---|---|---|---|
| 1 | Body read has no deadline (`http.ts`) | **Yes.** A test publisher that sends headers and then drips one byte at a time held the reviewed code past 5 s with a 1 s timeout. | **Fixed.** One abort signal now covers the request, the headers and the whole body. Its budget is the smaller of the per-request timeout and the time left to the run deadline. `readBounded` races every read against the signal and cancels the stream on any exit. A timed-out body is logged as `timeout` and retried only while the run deadline allows. | `ingest/test/http_review.test.ts`: "slow-drip BODY is cut off… stream is cancelled", "the RUN deadline bounds the body read too". Function re-proved under the Deno edge runtime locally (HTTP 200 readback, 401 without the secret, 400 with a `url` field). |
| 2 | Fixed-password login on an automatic seed path | **Yes.** `[db.seed]` ran `seed.sql` on every reset, and the CLI applies seeds on `db reset --linked` as well. | **Fixed by removal, not by a guard inside the seed.** `supabase/seed.sql` is deleted and `[db.seed] enabled = false`. No migration or config sets any role password (tested). The local worker login is created by an explicit step, `node ingest/src/local_bootstrap.ts`: no target option at all; the only route is `docker exec` into this repository's local CLI container; a remote Docker daemon, a non-default Docker context or a non-local database URL in the shell is refused; the SQL itself refuses unless it arrived over the server's own unix socket (`inet_server_addr() is null`), which no network client can satisfy; the password is random per run, sent only as a SCRAM verifier, stored in a git-ignored owner-only file, and the login expires after 14 days. The runbook states that `db reset --linked` is forbidden. | `ingest/test/operator.test.ts`: "the local bootstrap refuses every remote target" (8 remote shapes), "bootstrap SQL leads with the server-side socket guard". `ingest/test/integration.test.ts`: "no automatic seed path creates a login…", and the same SQL sent over TCP is refused by the server. CI calls the bootstrap explicitly (`tools/workflow.test.ts`). |
| 3, 4 | Secrets as `psql -v` arguments and in statement logs | **Yes**, both. `\prompt` plus `set local log_statement` (the suggested fix) was **not sufficient**: `set local log_statement` needs a privilege a hosted `postgres` role may not have, and it does nothing about pgaudit, duration logging, or parameter logging on error. | **Fixed with a TypeScript operator tool**, `ingest/src/operator.ts`; the two SQL scripts are deleted. A secret is never an argument (refused before anything else, including a connection string carrying a password); input is a hidden, confirmed terminal prompt or the first line of standard input. **Role password:** never sent. A SCRAM-SHA-256 verifier is computed client-side, so the most a statement or audit log can hold is a salted, iterated hash; if such logging is in force and cannot be switched off for the transaction the tool stops unless `--accept-logged-verifier` is given, and then requires 32+ characters. **Vault secret:** must be sent (Vault encrypts server-side), so it goes only as a bind parameter of a fixed statement, only after the tool has read back `log_statement`, duration logging, `log_parameter_max_length`, `log_parameter_max_length_on_error`, `pgaudit.log` and `pgaudit.log_parameter` as actually in force; if any would record parameters the tool **refuses and sends nothing**. Output is role names, entry names and timestamps only. | `ingest/test/operator.test.ts` (10 tests): argument refusal; hidden/confirmed input; "the server is sent a SCRAM verifier only"; refusal without the flag; "REFUSED and the secret is never sent" under three logging configurations (the stub proves the value never reached the server). Run against the real local server: Vault entries set from stdin, a login created and rotated, sign-in with the plaintext succeeded (so the verifier is right), and neither secret appears in the database container's log (count 0). |

## Should-fix: red lines, publisher access, rights

| # | Finding | Verified? | Disposition | Evidence |
|---|---|---|---|---|
| 5 | Generic browser makes vote columns sortable (R1) | **Yes**, latent (values are null today). | **Fixed, default deny.** In the generic browser a column is a sort key only if its type is not numeric **and** no word of its name is a tally, share, seat count, rank, total, amount or confidence (`web/src/lib/generic-sort.ts`). The URL validator drops such a key before a request is built; the page narrows again to the columns it offers and removes a rejected key from the address bar; the header of a figure column is plain text, not a control. The page note says figures are shown as the source reported them and cannot order rows. It deliberately does **not** describe results as scores or as any measure of accuracy. | `web/src/lib/lib.test.ts` "R1 in the generic dataset browser" (3 tests). `web/e2e/browse.spec.ts`: `?sort=party_votes&dir=desc` produces no `order=` on any result column, the key leaves the URL, numeric headers carry no sort button. |
| 6 | Spoofed `Origin`/`Referer`, undocumented endpoint, no pacing | **Yes.** Also checked for a documented route: the bills site is a single-page app, no published API description or feed was found (two obvious documentation addresses answered 404). | **Fixed, fail closed.** Headers removed, and the fetch guard now strips `Origin`, `Referer`, `User-Agent`, `Cookie`, `Authorization`, `Host` and forwarding headers from any adapter. Sources declare an `access_basis`; `undocumented_endpoint` can never be enabled (config validation and a table constraint) and the runner blocks the run **before any request** (`access_basis_not_established`). `nz_parliament_current_bills` is classed that way, disabled with its blocker written out, and its schedule removed. No permission was invented: the blocker says a documented route or written permission is needed. Pacing: every request to a host waits a per-source minimum interval (default 1.5 s, configurable 1–60 s) across the whole run. | `ingest/test/adapters.test.ts` "bills endpoint is classed undocumented… sends no Origin or Referer"; `http_review.test.ts` "requests to one host are paced across the whole run"; integration "an undocumented endpoint is never contacted" (0 calls, live and dry run). Live receipt: `blocked / access_basis_not_established`, no fetch entries. |
| 7 | No `robots.txt` handling | **Yes.** | **Fixed.** Per host, once per run, through the same guard and into `fetch_log`: RFC 9309 matching (this client's token, then `*`; longest match; Allow wins ties); 404/410 means no rules; anything unreadable (refused, challenged, failing) means access is **not** assumed; `Crawl-delay` raises the pacing interval, and one a bounded run cannot honour blocks the run. Outcomes `robots_disallowed`, `robots_unavailable`, `robots_crawl_delay_exceeds_budget` end the run as **blocked**, never as "no records". **Consequence found by doing this:** the MP directory host publishes `User-agent: *` / `Disallow: /`. The adapter now honours that; the source is disabled with that blocker and its schedule removed. The earlier receipts from that host predate the check and are kept as history, labelled. | `http_review.test.ts` (3 robots tests); integration "a robots.txt disallow ends the run as blocked, is stored in fetch_log". Live receipts: MP directory `blocked / publisher_robots_disallowed`; 2023 results host `publisher_robots_unavailable` (its robots.txt answers 403). |
| 8, 9 | Live sources without `rights_id`; register has no terms URLs or dated checks | **Partly.** Missing ids: yes. The suggested ids were **not** right: RIGHTS-02 is candidate expenses and RIGHTS-03 is the 2023 historical pages. | **Fixed, human step remains.** `rights_id` and `access_basis` are required for `live_fetch` in `registry.ts` (config error) and by a table constraint. Two **pending, link-only** rows were added, RIGHTS-20 (register of political parties) and RIGHTS-21 (2026 election information); no existing row was approved or re-dated. **Terms and robots checks are recorded as provenance, not as approval:** `ingest/src/access_check.ts` retrieves robots.txt and, where robots allows, the register's terms page, and stores URL, status, size, body hash and finding in the append-only `publisher_access_checks`; the table has no field that could pass for a legal conclusion. **Activation now needs a person:** `activate_schedule()` and every dispatch call `automated_access_blocker()`, which requires a rights row, a terms URL in the register, a `publisher_terms_reviews` row by a named person that rests on a recorded retrieval of that exact page and permits automated access, not older than a year, and a robots check from the last 30 days that allows the path. No tool, seed or migration writes a terms review, and the worker role cannot. | pgTAP `050` (14 new assertions: refused without URL, without review, with retrievals only, with an "unclear" reading; a review cannot rest on an unretrieved page; append-only; proof names the review; a later robots disallow stops an active schedule at dispatch). pgTAP `030`: worker may record a check, may not write a review. Real state, local: with a genuine function readback and Vault secrets in place, activation of `releases-feed-hourly` was **refused** ("the rights register records no terms URL for this publisher"); 0 cron jobs. Receipt `*-publisher-access-checks.json`. |
| 10 | No confidence value on summaries (R9) | **Yes.** | **Fixed.** `confidence` (0–1, nullable), `confidence_status` (`reported` / `not_reported` / `not_applicable`, default `not_reported`) and `confidence_basis`. Constraints: a value exists exactly when status is `reported`; a reported value must say what the number is; the fields are part of the immutable output. Unknown is never 0 and nothing is defaulted or estimated. Shown in the `summaries` view and the summary card, with a line saying the figure is about the text, not about any person. | pgTAP `090` (9 assertions, including "unknown, zero and not-applicable stay three different things"); vitest `formatConfidence`; component test. |
| 11 | Model policy classifications have no model run | **Yes.** | **Fixed.** `policy_sources.model_run_id` with `(classification_basis = 'unreviewed_model') = (model_run_id is not null)`, the same confidence fields, and a rule that only non-model labels may be `not_applicable`. New view `policy_classifications` carries model, version, schema version and confidence state. The R9 label columns are classed as unconditional link metadata, so a rights row can never release a class while hiding its model label. **Beyond the comment:** R9's "documented human-agreement rate for that schema" is now its own append-only table, `schema_agreement_validations` (per schema version, sample size, agreements, computed rate, method link). It is distinct from per-output `review_decisions`: approving one output does not clear the "not yet checked against human review" flag, and a study approves no output. The UI previously cleared that flag on per-output approval; that was wrong against R9 and is fixed. | pgTAP `090` (14 assertions); vitest "keeps the human-review flag until an agreement study exists"; component tests. |
| (no line) | Red-line scan skips TS/TSX copy | **Yes.** Adding the path to the Python line scanner would have been wrong both ways: it misses JSX text split over lines and trips on identifiers, comments and the regexes in the code. | **Fixed with a purpose-built scanner**, `tools/red_lines_copy.ts` (the pinned `typescript` package ships no JavaScript compiler API). It tests only reader-facing text: string literals, template text, JSX text and rendering attributes; never comments, identifiers, regex literals, module specifiers or non-rendering attributes. Sentences stay whole across inline elements and substitutions. The two patterns are held equal to `scripts/red_lines.py` by a test. Runs in CI. | `tools/red_lines_copy.test.ts` (7 tests: 9 regression shapes, split phrases, 14 false-positive shapes, extraction, the real explorer scanned with its actual sentences found and clean, exemptions, parity). Current scan: 60 files, about 2,280 text pieces, clean. |
| 12 | Footer: maintainer is not an accountable person (R8) | **Yes.** | **Fixed, human step remains.** Acceptance of the accountable-person role is a human decision and **none has been recorded, so none is claimed**. The footer keeps the maintainer/contact line, adds a contact route (the public issue tracker, so no private address is published), and states plainly: "Accountable person: not yet confirmed… the evidence in this explorer stays withheld until a named person has accepted it". The `r8_accountable_legal_entity` gate stays closed. When someone accepts, the label and name change in one component, with README and REVIEW-REGISTER.md. | Component test asserts the statement, that the maintainer is not presented as that person, and the contact link. |

## Nits

| # | Finding | Disposition | Evidence |
|---|---|---|---|
| 13 | Row counts and column names of withheld tables | **Fixed** for counts: `approximate_rows` is null for any withheld table; the table is still listed with its reason (nothing silently omitted). **Not changed** for column names: every column of every table is already public in this repository's migrations, so hiding them in the catalogue would protect nothing and would break the "documented, never silent" rule. | pgTAP `090`, as `anon`. |
| 14 | Worker can repoint an active schedule | **Fixed.** UPDATE policy is `using (state = 'inactive') with check (state = 'inactive')`. `sync_schedules()` skips an active schedule whose config is unchanged and **refuses** changed config until it is deactivated. | pgTAP `030` (active row unchanged after the worker's UPDATE; inactive row editable; cannot self-activate), `050`. |
| 15 | `http:` accepted in production | **Fixed.** https only. Plain http is accepted in exactly one case: the build is explicitly marked `VITE_LOCAL_TEST_STACK=1` **and** the host is loopback; the flag cannot make a remote http URL valid. Set only in the Playwright config. `check-bundle` fails any `http://` or `ws://` in `connect-src` outside that case. | vitest "https only" (2 tests). |
| (no line) | Older workflows tag-pin checkout and persist credentials | **Fixed.** Pinned to the verified `v7.0.1` commit with `persist-credentials: false`. | `tools/workflow.test.ts`. |
| 16 | Dead `peopleSpec` and `tab` filter | **Fixed**, removed with `peopleRouteSpec`. | vitest. |
| 17 | `identitiesSpec` sorts by per-person counts | **Fixed.** Counts are shown, not sortable. | vitest. |
| 18 | Custom headers re-sent on redirect | **Fixed**, more strictly than asked: adapter headers and the body are kept only while the **origin** is unchanged. | `http_review.test.ts`. |
| asked by owner | Privilege tests for register writes | **Added.** The worker cannot insert, update or delete any projection register, cannot run `classify_public_columns()` or `rebuild_exposed_views()`, cannot create a view in an exposed schema or alter a private table. | pgTAP `030`. |
| asked by owner | FORCE ROW LEVEL SECURITY | **Evaluated, not added.** FORCE only changes behaviour for a table **owner** that lacks BYPASSRLS. Every private table is owned by the migration role, which has BYPASSRLS locally and on hosted Supabase, so FORCE would be a no-op today. It would not be harmless in future: if the platform ever removed BYPASSRLS from that role, FORCE would make operator steps (gates, memberships, reviews) silently match zero rows instead of failing. What actually matters is tested instead, so the decision is revisited if a premise stops holding: RLS enabled on every private table; no private table owned by a role a client or the worker can become; no view owned by the table owner; no SECURITY DEFINER function. | pgTAP `090`, last four assertions. |

## Process points

| Claim | Disposition |
|---|---|
| "Branch protection on `main` is still off." | **Corrected; not accurate.** Read-only check on 2026-09-20: protection is **on** — administrators included, 1 approving review, code-owner review required, required checks `red-lines` and `offline-validation`, strict up-to-date, no force pushes or deletions. What is true is narrower: the newer `explorer.yml` checks are not yet *required*. That is an owner decision, recommended in [branch-protection.md](branch-protection.md). Remote settings were not changed by this work. |
| Sole CODEOWNER, so no second pair of eyes | **Accurate; deferred to the owner.** Code cannot appoint a second reviewer, and none is invented. |
| 182 files is beyond a line-by-line read; the PR should say the merge rests on tests and gates | **Accurate; deferred to the coordinator**, who owns the PR text. Current counts are in the checklist. |
| The register should say which reviews were human | **Accurate; no change needed to the register.** Every REVIEW-REGISTER.md row is PENDING with nobody appointed. The reviews of this branch so far were automated, none is a legal review, and none is recorded as an approval. |
| Direction: this is not the #4–#7 spike | **Not a defect of this change; scope unchanged.** The owner asked for this infrastructure explicitly and separately from the spike. It publishes nothing. Whether it merges before #5 and #6 exist is the group's decision. |

## Second look at the fixes themselves

Before verification the new security-sensitive code was read again, adversarially, by a separate automated reviewer (not a human review, not a sign-off). It reported no high-severity defect and nine lower ones. Each was checked; all nine were valid and are fixed with tests:

| Finding | Fix | Test |
|---|---|---|
| The copy scanner lost JSX mode after an element given as an attribute value (`fallback={<X rows={5} />}`), then skipped the text that followed. Reproduced: a phrase on the next line was missed. | Entering `{` or `${` resets the scanner's "what came before" state. | `red_lines_copy.test.ts` "JSX given as an attribute value…" (5 shapes, including that the rest of the file is still scanned). |
| The request timer started before the per-host pacing wait, so a `Crawl-delay` longer than the timeout aborted every later request unsent and logged the publisher as timing out. | Pace first, then start the budget. | `http_review.test.ts` "waiting for our turn… does not eat the request's own time budget". |
| Robots agent groups matched by substring: a group for an unrelated agent could override `*`, and `token/1.0` was missed. Wildcard rules compiled to a backtracking regular expression. | Exact token or token plus version; piecewise linear matcher. | `http_review.test.ts` "robots agent groups match our product token only…" (14 pattern cases and a timing bound). |
| Bootstrap: a remote context chosen with `docker context use` sets no variable; hostnames were pattern-matched (`postgresql://localhost:pw@remote…` passed); the container check was a tautology; the SQL guard was a separate block that only stopped the script under stop-on-error. | The Docker CLI is asked for its active endpoint and must answer a local socket; hostnames are parsed; the container comes from the daemon's list; the SQL is one block whose first statement is the guard. | `operator.test.ts` (7 remote URL shapes, 4 endpoints, container selection, single-block SQL). |
| Parameter logging was missed for sampled transactions, `auto_explain`, and pgaudit object auditing; a failing role statement is logged with its text. | All four are read back and treated as risks. | `operator.test.ts` (three further refused configurations). |
| A check dated in the future could mask later robots checks and never go stale. | The server stamps `recorded_at` (and `reviewed_at`), future dates are refused, "latest" is by `recorded_at`. | pgTAP `050`. |
| A dataset made only of figures fell back to its first column as the tiebreak; type test was a list of numeric spellings. | No ordering is sent; the type test is now an allowlist (identifiers, text, dates, booleans), so arrays, json, ranges, domains and unknown types are refused. | vitest "a dataset made only of figures…". |

## Things this review made visible that are not "done"

- **Live adapters in use fell from three to one.** The releases feed is the only source this client may fetch on the evidence recorded. The MP directory is blocked by the publisher's robots.txt; bills has no documented route. Both need the publisher's permission or a documented route: a person's job.
- **No schedule can activate** until a person records a terms URL and a terms review for that source. For the releases feed the terms page answered this client with a bot challenge, so no URL was verified and none was entered.
- **The Parliament terms URL** recorded in the register was confirmed to exist by one retrieval made before that host's robots.txt had been read. It is kept as a link for human readers; the tool does not fetch it, and records that it is not retrievable by an automated client.
- R8 acceptance, R10 review, every rights row, and the security re-review remain open human gates. **Security is not signed off.**
