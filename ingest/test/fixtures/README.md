# Test fixtures

Everything in this directory is synthetic and exists only for automated tests. The names, parties,
electorates, bill titles, identifiers and numbers are invented. None of it is evidence and none of it
is ever loaded into a shared or hosted database. Fixture values are labelled `TEST FIXTURE` or use
`Fixture`/`fixture_` names so they cannot be mistaken for live data.
